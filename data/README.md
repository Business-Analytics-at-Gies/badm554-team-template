# data/

Local data files go here — downloaded course databases, working extracts, anything large or private.

**Nothing in this folder is committed.** The repo's `.gitignore` excludes it (this README is the one exception, so the folder exists when the repo is cloned). Each teammate downloads course data files themselves; the assignment pages carry the download links and checksums.
